审查已完成，但**写入报告文件的操作需要你在权限提示中批准**——我连续三次调用 Write（以及 `node -e`、`mkdir`）都返回"尚未授予权限"，在你批准写入 `docs/` 之前我无法落盘，因此也无法执行后续的 `wc -l` / `head` 核对。

请在权限提示里允许写入 `/home/LittlePigeno/WorkSpace/Projects/fnos-apps/docs/p115-audit-20260918.md`（或允许 Bash 写文件），我会立即落盘并核对行数。

为避免结论丢失，先把核心结论贴出：

## 总览
工程质量较高、鉴权/加密边界清晰（cookie/token Fernet 落盘、302 HMAC 常量时间验签、命令执行走参数数组白名单、路径穿越普遍 resolve+startsWith、前端统一转义）。无可被未授权远端直接利用的严重漏洞。**评分 7.5/10。P0：0 个；P1：3 个；P2：4 个。**

## P1（3 个）
1. **302 限流形同虚设（已修复项回归）** — `main.js:319-322` + `:371/:414`：限流 Map 挂在**每请求新建**的 `TrimHandler` 实例上，桶永远从 `n=0` 开始，60req/60s **完全不生效**。3667 匿名端口无频率保护，易致 115 账号被风控。修复：限流 Map 提升为进程级共享。
2. **自更新无 SHA-256/MD5 校验** — `update.js:189-256`：清单 `latest.json` 已含 sha256/md5，但 `downloadUpdate` 只校验 size，`applyUpdate` 直接 install-fpk。供应链软肋。修复：落盘后严格比对 sha256。
3. **大文件 SHA-1 同步读取阻塞事件循环** — `client.js:1122-1146`（`_calcSha1` 全文件 `fs.readSync`）：上传大文件期间整个 Node 事件循环冻结，302 播放/API 假死。修复：改流式异步哈希。

## P2（4 个）
1. `client.js:360/369` — `generateQrcode` 对 `const data` 赋值，legacy 回退路径抛 `TypeError`。
2. `records.js:22-25` — `statSync` 未用 `{bigint:true}`，`mtimeNs` 恒 undefined，增量实际只按文件大小判断，与文档不符。
3. `server.js:831-838` — `_authorizedLocalPath` 的 R_OK 回退放宽授权边界。
4. `server.js:1704-1729` — STRM 增量清理会删除 target 目录下所有非本轮预期的 `.strm`，误配即误删既有文件。

## 已修复项复核
- RSA modPow 快速模幂 ✅
- 60req/60s 按 IP 限流 ❌（见 P1-1，已回归失效）
- check_update/download_update 的 ok() 解包 ✅
- raw.githubusercontent 更新源 ✅（但缺完整性校验，见 P1-2）
- 302 HEAD/Location 编码 ✅
- 302 签名 HMAC 验签 ✅（`server.js:2285-2296` timingSafeEqual + 长度守卫）

批准写入后我会把完整报告（含 P3 与重点风险清单）落盘并回报路径、行数与 P0/P1 数量。
EXIT:0
